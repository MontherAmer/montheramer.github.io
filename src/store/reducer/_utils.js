import actionTypes from '../actions_types';

const initialState = {
  unit: 'metric'
};

export default (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
